{
  "name": "lerna-drone-poc",
  "private": false,
  "author": "PauloIVM",
  "license": "ISC",
  "version": "0.0.1",
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "jest": "^24.1.0",
    "lerna": "^3.13.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run tsc",
    "clean-build": "rm -rf node_modules && npm i && lerna clean && npm run bootstrap && npm run build",
    "publish": "lerna publish --conventional-commits --cd-version=$LERNA_LEVEL --yes --no-changelog",
    "test": "jest --verbose --runInBand --detectOpenHandles --forceExit",
    "test-update-snapshot": "jest -u --runInBand"
  },
  "dependencies": {
    "@types/node": "^14.14.7"
  }
}
